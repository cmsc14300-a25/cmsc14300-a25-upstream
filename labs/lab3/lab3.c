/*
 * CMSC 14300
 * Autumn 2025
 * Lab #3 Deliverables - C file
 *
 * You will modify this file as part of the lab.
 */

#include "lab3.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>

/* Task 1 -- see lab3.h for the header comment */
int count_occurrences(int nums[], int N, int val) {
  assert(N > 0);

  // YOUR CODE HERE
  // Replace -1 in the return statement with an appropriate value
  return -1;
}

/* Task 2  -- see lab3.h for the header comment */
int div_and_mod(int x, int y, int *remainder) {
  assert(y != 0);
  assert(remainder != NULL);

  // YOUR CODE HERE
  // Replace -1 in the return statement with an appropriate value
  return -1;
}

/* Task 3  -- see lab3.h for the header comment */
void find_first_last_occurrence(int nums[], int N, int val, int *first,
                                int *last) {
  assert(N > 0);
  assert(first != NULL);
  assert(last != NULL);

  // YOUR CODE HERE
}

/* Task 4  -- see lab3.h for the header comment */
int *compute_frequencies(int lb, int ub, int nums[], int N, int *result_len) {
  assert(lb >= 0);
  assert(ub >= lb);
  assert(N > 0);
  assert(result_len != NULL);

  // YOUR CODE HERE
  // Replace NULL in the return statement with an appropriate value
  return NULL;
}
